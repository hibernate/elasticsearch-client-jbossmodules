<?xml version='1.0' encoding='UTF-8'?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.hibernate.elasticsearch-client-jbossmodules</groupId>
        <artifactId>elasticsearch-client-jbossmodules-parent</artifactId>
        <version>5.6.5.hibernate02</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>elasticsearch-client-jbossmodules</artifactId>
    <packaging>pom</packaging>

    <name>Elasticsearch Client JBoss Modules</name>
    <description>Elasticsearch Client JBoss Modules packaging</description>

    <properties>
        <featurepack.dir>${project.build.directory}/feature-pack/src/main/resources</featurepack.dir>
        <template.dir>${featurepack.dir}/modules/</template.dir>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.elasticsearch.client</groupId>
            <artifactId>elasticsearch-rest-client</artifactId>
        </dependency>
        <dependency>
            <groupId>org.elasticsearch.client</groupId>
            <artifactId>elasticsearch-rest-client-sniffer</artifactId>
        </dependency>
        <dependency>
            <groupId>org.wildfly</groupId>
            <artifactId>wildfly-feature-pack</artifactId>
            <type>zip</type>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <execution>
                        <id>gen-module-elasticsearch-rest-client</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${template.dir}/system/layers/base/org/elasticsearch/client/elasticsearch-rest-client/${slot.full.id}/</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>src/main/modules/elasticsearch-rest-client</directory>
                                    <filtering>true</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                    <execution>
                        <id>gen-alias-elasticsearch-rest-client</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${template.dir}/system/layers/base/org/elasticsearch/client/elasticsearch-rest-client/${slot.simple.id}/</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>src/main/modules/aliases/elasticsearch-rest-client</directory>
                                    <filtering>true</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                    <execution>
                        <id>gen-module-elasticsearch-rest-client-sniffer</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${template.dir}/system/layers/base/org/elasticsearch/client/elasticsearch-rest-client-sniffer/${slot.full.id}/</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>src/main/modules/elasticsearch-rest-client-sniffer</directory>
                                    <filtering>true</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                    <execution>
                        <id>gen-alias-elasticsearch-rest-client-sniffer</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${template.dir}/system/layers/base/org/elasticsearch/client/elasticsearch-rest-client-sniffer/${slot.simple.id}/</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>src/main/modules/aliases/elasticsearch-rest-client-sniffer</directory>
                                    <filtering>true</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                    <execution>
                        <id>gen-feature-pack-buildfile</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${featurepack.dir}</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>src/main/feature-pack-def</directory>
                                    <filtering>true</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.wildfly.build</groupId>
                <artifactId>wildfly-feature-pack-build-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>feature-pack-build</id>
                        <goals>
                            <goal>build</goal>
                        </goals>
                        <phase>compile</phase>
                        <configuration>
                            <config-file>feature-pack-template.xml</config-file>
                            <config-dir>${featurepack.dir}</config-dir>
                            <resourcesDir>/</resourcesDir>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <configuration>
                    <finalName>${project.artifactId}-${project.version}</finalName>
                    <descriptors>
                        <descriptor>${basedir}/assembly.xml</descriptor>
                    </descriptors>
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
