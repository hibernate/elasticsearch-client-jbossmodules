#This is to let Maven properties be injected in the file, making it easier to run tests in the IDE as well
elasticsearch-rest-client.fullversion = ${version.elasticsearch}
elasticsearch-rest-client.slot.id = ${slot.simple.id}
elasticsearch-rest-client-sniffer.fullversion = ${version.elasticsearch}
elasticsearch-rest-client-sniffer.slot.id = ${slot.simple.id}
jsonassert.fullversion=${version.jsonassert}
